package ph.sinonet.vg.live.parser;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public class SmsParser {
	private final static Log log = LogFactory.getLog(SmsParser.class);
	
}
